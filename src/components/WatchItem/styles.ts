import {ViewStyle} from 'react-native';

const styles = {
  container: {
    flexDirection: 'row',
    padding: 20,
    elevation: 5,
    backgroundColor: '#ffffff',
    alignItems: 'center',
    justifyContent: 'space-between',
    borderRadius: 5,
  } as ViewStyle,
};

export {styles};
